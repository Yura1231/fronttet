worker_processes 1;
events {
    worker_connections 1024;
}
http {
    include mime.types;
    sendfile on;

    server {
        listen 80;
        server_name _;
        root /app/public;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
